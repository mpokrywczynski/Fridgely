name: Build & Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo_mysql, tokenizer, xml, ctype, json, bcmath

      - name: Install PHP dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction

      - name: Install JS dependencies
        run: npm ci

      - name: Build assets
        run: npm run build
        env:
          NODE_ENV: production

      - name: Generate setup.php
        id: setup
        run: |
          TOKEN=$(openssl rand -hex 32)
          echo "token=$TOKEN" >> $GITHUB_OUTPUT
          sed "s/%%TOKEN%%/$TOKEN/" .github/setup-template.php > public/setup.php
          echo "setup.php generated"

      - name: Deploy via SFTP (lftp)
        run: |
          sudo apt-get install -y lftp -qq
          lftp -c "
          set sftp:auto-confirm yes
          set net:max-retries 5
          set net:timeout 60
          set net:reconnect-interval-base 5
          set net:reconnect-interval-multiplier 2
          open -u '${{ secrets.SFTP_USER }}','${{ secrets.SFTP_PASSWORD }}' sftp://${{ secrets.SFTP_HOST }}
          mirror --reverse --delete --no-perms \
            --exclude-glob .git/ \
            --exclude-glob .github/ \
            --exclude-glob node_modules/ \
            --exclude-glob vendor/ \
            --exclude-glob .env \
            --exclude-glob .env.* \
            --exclude-glob .claude/ \
            --exclude-glob storage/logs/ \
            --exclude-glob storage/framework/sessions/ \
            --exclude-glob storage/framework/cache/ \
            --exclude-glob storage/framework/views/ \
            ./ ${{ secrets.SFTP_PATH }}
          echo '=== public/ on server after mirror ==='
          ls ${{ secrets.SFTP_PATH }}/public/
          echo '=== bootstrap/cache/ on server ==='
          ls ${{ secrets.SFTP_PATH }}/bootstrap/cache/
          bye
          "

      - name: Run setup.php on server
        run: |
          echo "Triggering setup..."
          HTTP_CODE=$(curl -s -o /tmp/setup_response.txt -w "%{http_code}" --max-time 90 \
            "${{ secrets.APP_URL }}/setup.php?token=${{ steps.setup.outputs.token }}")
          RESPONSE=$(cat /tmp/setup_response.txt)
          echo "HTTP status: $HTTP_CODE"
          echo "Response: $RESPONSE"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "::error::Setup failed with HTTP $HTTP_CODE"
            exit 1
          fi
          echo "Deploy complete."
